<h2>ユーザー一覧</h2>

<table>
  <thead>
  <tr>
    <th>Eメール</th>
    <th>名前</th>
  </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= user.name %></td>
    </tr>
  <% end %>
  </tbody>
</table>

