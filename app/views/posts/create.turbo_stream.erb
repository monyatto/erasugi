<%# `#cats`の先頭に登録されたcatを追加する %>
<%= turbo_stream.replace 'new' do %>
  <%= render 'posts/show'  %>
<% end %>

<%#= turbo_stream.replace "new", partial: "posts/test" %>

<%# 登録の入力フォームの中身を空にする %>
<%#= turbo_stream.update Post.new, "" %>

<%# Flashを表示 %>
<%= turbo_stream.prepend "flash", partial: "layouts/flash" %>
